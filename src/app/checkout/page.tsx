import React from "react";
import CheckoutTemplate from "@/components/templates/CheckoutTemplate";

export const metadata = {
  title: "Checkout / Audiophile Shop",
};

const CheckoutPage = () => {
  return (
    <div>
      <CheckoutTemplate />
    </div>
  );
};

export default CheckoutPage;
